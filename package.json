{
  "name": "bunradio",
  "module": "src/index-rtmp.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index-rtmp.ts",
    "start": "bun run src/index-rtmp.ts",
    "build": "bun build --compile --outfile=bunradio src/index-rtmp.ts",
    "mcp": "bun run src/mcp-server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "mcp-lite": "^0.10.0",
    "zod": "^4.4.3"
  }
}